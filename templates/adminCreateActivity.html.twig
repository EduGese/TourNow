{% extends 'base.html.twig' %}
{# FORMULARIO DE REGISTRO DE CREACION DE ACTIVIDAD Creado desde---->   Form/CreateActivityFormType.php



{# Pretendia que esta vista se extendiera de adminDashboard.html.twig para que el formulario apareciera a 
continuacion de la lista de actividades, pero al hacerlo solo extiende de base.html.twig..No se por que #}

{# {% block stylesheets %}
	<style>
		.mapa{
			display: inline-block;
			margin: auto;
			width: 50%;
		}
		#mapa{
			float:right;
		}
	</style>
{% endblock %} #}
{% block body %}
	

	<div class=" row">

	{% for flashMessage in app.flashes('fecha_erronea') %}
		{{ flashMessage}}
	{% endfor %}
	{% for flashMessage in app.flashes('nombre_duplicado') %}
		{{ flashMessage}}
	{% endfor %}
	<div class= " a-form col-lg-5 col-md-12 mr-auto ml-auto ">
		{{ form_start(form) }}
		<h1> Crea una Nueva Actividad</h1>
		{{ form_row(form.activity_name) }}
		{{ form_row(form.description) }}
		{{ form_row(form.tickets) }}
		{{ form_row(form.start_ubication) }}
		{{ form_row(form.end_ubication) }}
		{{ form_row(form.start_coord) }}
		{{ form_row(form.end_coord) }}
		{{ form_row(form.price) }}
		{{ form_row(form.image) }}
		{{ form_row(form.date) }}
		{{ form_row(form.city) }}
		{{ form_row(form.company_name, {'value': app.user.companyname}) }}

		{{ form_row(form.company_website, {'value': app.user.companywebsite}) }}

		<button type="submit" class="boton btn a-btn">Guardar</button>
		{{ form_end(form) }}

		

	</div>
	<div class="mapa col-lg-5 col-md-12 mr-auto ml-auto" id="mapa">
	<div class=" m-ayuda" >
	<i class="fa-solid fa-exclamation" style=" font-size:36px;"></i>
	<h4>Para poner la primera ubicación pincha en el mapa.Vuelve a pinchar para la final.</h4>
	</div>
		<div id="map" style="height: 500px; width: 100%;"></div>
		<div class="btn a-botn">
		<button type="  button"  class="boton a-btn"id="clear">Borrar seleccion</button> 
		</div>
	</div>
	</div>
	
	{# {% block javascripts %}
		<script src="{{ asset('/scripts/mapa.js') }}">
		</script>	
	
	{% endblock %} #}
{% endblock %}
