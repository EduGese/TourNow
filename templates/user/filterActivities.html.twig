{% extends 'base.html.twig' %}

{% block body %}
    <h1>Filter Activities</h1>
    
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit">Buscar</button>
    {{ form_end(form) }}
{% endblock %}
